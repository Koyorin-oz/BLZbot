{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Push main → déclenche déploiement Pebble (GitHub Actions)",
      "type": "shell",
      "command": "git push origin main",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      },
      "problemMatcher": []
    }
  ]
}
